// eslint-disable-next-line no-unused-vars
const Discord = require('discord.js');
const SA = require('superagent');
const fs = require('fs');

module.exports = {
	name: 'execute',
	aliases: ['e'],
	perm: 268435456n,
	dm: true,
	takesFirstArg: false,
	// eslint-disable-next-line no-unused-vars
	async execute(msg) {

	}
};