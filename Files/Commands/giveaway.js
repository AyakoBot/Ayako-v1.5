const Discord = require('discord.js');

module.exports = {
	name: 'giveaway',
	perm: null,
	dm: false,
	takesFirstArg: false,
	aliases: ['g'],
	async exe(msg) {
        if (msg.args[0]) {

        } else {
            
        }
	} 
};